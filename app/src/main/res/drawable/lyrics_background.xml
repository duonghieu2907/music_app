<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background Gradient -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="180"
                android:startColor="#39C0D4"
                android:endColor="#7CEEFF"
                android:type="linear" />
            <corners android:radius="25dp" />
        </shape>
    </item>

    <!-- Inner Shadow -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="25dp" />
            <padding
                android:left="0dp"
                android:top="0dp"
                android:right="0dp"
                android:bottom="0dp" />
            <innerRadiusRatio
                android:innerRadiusRatio="3.0"
                android:useLevel="true" />
        </shape>
    </item>

    <!-- Shadow Effect -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#000000" />
            <corners android:radius="25dp" />
        </shape>
    </item>

</layer-list>
